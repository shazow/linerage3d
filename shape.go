package main

/*

import (
	"bytes"

	"golang.org/x/mobile/gl"
)

const vertexDim = 3
const textureDim = 2
const normalDim = 3

type Shape struct {
	glBuf gl.Buffer
	glTex gl.Texture

	vertices []float32 // Vec3
	textures []float32 // Vec2 (UV)
	normals  []float32 // Vec3
}

func (s *Shape) Buffer(glBuf gl.Buffer) error {
	s.glBuf = glBuf

	buf := bytes.Buffer{}

	//for
	//err := binary.Write(&buf, binary.LittleEndian, v)
	//}

	gl.BufferData(gl.ARRAY_BUFFER, EncodeObject(cubeData), gl.STATIC_DRAW)
}

// TODO: Good render loop: http://www.java-gaming.org/index.php?topic=18710.0
*/
